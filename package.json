{
  "name": "prescription-builder-backend",
  "version": "1.0.0",
  "description": "REST API for the Prescription Builder doctor portal",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "express",
    "mongodb",
    "prescription",
    "doctor"
  ],
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.1",
    "morgan": "^1.10.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
